import { FormExpirationDate } from './form-expiration-date';

describe('FormExpirationDate', () => {
  it('should create an instance', () => {
    expect(new FormExpirationDate()).toBeTruthy();
  });
});
